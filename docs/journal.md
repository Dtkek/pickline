# Журнал сессий

Общая память для Claude Code на Mac и ПК. Каждая сессия при старте читает
последнюю запись (это делает хук автоматически), а в конце работы дописывает
свою — **новые записи снизу**, коммит и push. Формат записи:

```
## ДАТА · машина (mac / pc) · коротко о чём сессия

**Сделано:** что изменилось и где (файлы, коммиты).
**Проверено:** что реально запускалось и что показало.
**Открыто:** что не доделано, что сломано, какие гипотезы.
**Следующий шаг:** одна строка — с чего начинать следующей сессии.
```

Запись — не отчёт и не пересказ диалога: только то, что нужно, чтобы
продолжить с другой машины. Подробности — в коммитах и `docs/handoff_*.md`.

---

## 2026-09-14 · mac · STRATZ, собственное окно, exe для Windows

**Сделано:** сборка exe через GitHub Actions (релиз `latest`); STRATZ как
второй источник матчапов — снимок `app/data/stratz_fallback.json.gz`,
синергия, ранги, позиции по данным, еженедельное обновление снимка
(`update-snapshots.yml`, секрет `STRATZ_TOKEN` добавлен); собственное окно
приложения на pywebview (`app/window.py`) с «поверх окон»; обход кириллицы
в пути для pythonnet (копия `Python.Runtime.dll` в `%TEMP%`); передача
`docs/handoff_2026-09-14.md`.

**Проверено:** снимок STRATZ собран и согласован по рангам; подбор обоими
источниками; окно на macOS; на раннере GitHub exe открывает окно даже из
папки «Рабочий стол»; workflow снимка прошёл с IP GitHub.

**Открыто:** на ПК пользователя exe версии `.7` всё равно открывает
браузер, а не окно — вывод консоли не получен, причина неизвестна.
Гипотезы и команды — в `docs/handoff_2026-09-14.md`.

**Следующий шаг:** на ПК запустить `pickline.exe` из cmd, прочитать
вывод, починить окно.

---

## 2026-09-14 · pc · окно на Windows починено: дело было в метке MOTW

**Сделано:** причина «браузер вместо окна» найдена и устранена, версия `.8`.
Это не кириллица: обход `.7` отрабатывал верно, DLL бралась из копии по
ASCII-пути, и всё равно падало. Настоящая причина — метка «скачано из
интернета» (`Zone.Identifier`), .NET отказывается грузить помеченную
сборку (HRESULT `0x80131515`); помечены были 311 файлов из 314. Обход `.7`
не спасал, потому что `shutil.copytree` через `CopyFile2` переносит и
альтернативные потоки — метка ехала в ASCII-папку вместе с DLL.
`app/window.py`: `_dotnet_runtime_workaround` снимает метку со всей
`_internal` и с копии runtime (ASCII-обход оставлен). `build-windows.yml`:
проверка запуска сама метит файлы — CI этого не ловил, потому что сборка
на раннере локальная и метки на ней нет. `server.py`: подсказка про
«Разблокировать». `diagnose.py`: раздел 6 — pywebview, .NET, WebView2,
метки, кириллица в пути.

**Проверено на pc:** exe `.7` из «Рабочего стола» — снят вывод консоли,
`Failed to resolve …` при ASCII-пути копии; `Assembly::LoadFrom` дал
`0x80131515`; после `Unblock-File` сборка грузится и `Loader.Initialize`
резолвится; exe запущен заново — **окно открылось**, WebView2 поднялся,
страница пошла в API. `_unblock_tree` проверена на помеченных файлах
(снято 2 из 2). `diagnose.py` прогнан целиком, раздел 6 работает.
Сам exe версии `.8` ещё не собирался — это сделает CI.

**Открыто:** на ПК не было ни git, ни gh — поставлены через winget,
`gh auth login` сделан пользователем. Хук `session_start.sh` — shell-скрипт,
на Windows через PowerShell он не отработает, `git pull` пришлось делать
руками; если хук нужен на обеих машинах, ему нужен второй вариант.

**Следующий шаг:** дождаться сборки `.8`, скачать архив и проверить на ПК,
что окно открывается из скачанного архива без ручного `Unblock-File`.


## 2026-09-14 · mac · вечер: скорость подбора, exe без консоли, иконка, Pickline

**Сделано:** подбор больше не ждёт сеть — турнирная и личная составляющие
из памятки/снимка `app/data/tournament_fallback.json.gz`, пересчёт в фоне,
флаг `pending` и автодозапрос на странице (`tournament_fast`,
`net.get_json_fast`, `memo_*`). Ошибки OpenDota теперь читаемые (HTTP-код,
«не JSON, а страница ошибки»), одна повторная попытка на 429/5xx. Exe
собирается `--windowed`: вывод в `pickline.log` рядом с exe, падение и
откат в браузер — окном сообщения (`redirect_output_to_log`,
`message_box`). Иконка `app/icon.ico` (генератор `app/tools/make_icon.py`),
favicon и логотип в шапке. Репозиторий публичный (MIT, README в корне).
**Переименование в Pickline:** репозиторий `Dtkek/pickline` (старые
ссылки перенаправляются), exe `pickline.exe`, архив `pickline-windows.zip`,
лог `pickline.log`, заголовки. Имя GSI-конфига `…_drafthelper.cfg` оставлено
намеренно — он уже лежит у пользователей в папке Dota. CI: снимки STRATZ и
турниров одним workflow `update-snapshots.yml`; проверка запускает exe
через `-NoNewWindow` (иначе SmartScreen на помеченном exe отменял запуск).

**Проверено на mac:** подбор 2–4 мс с турнирами и аккаунтом (было — секунды
и минуты), фоновые памятки пишутся, favicon отдаётся. **На раннере:** exe
без консоли пишет лог, снимает метку с 312 файлов, окно открывается.
На ПК новая сборка не проверялась.

**Открыто:** хук `session_start.sh` на Windows не запускается (см. запись
ПК) — либо Claude Code не нашёл Git Bash (`CLAUDE_CODE_GIT_BASH_PATH`), либо
нужен второй вариант хука. Бэктест модели по-прежнему не сделан.

**Следующий шаг:** на ПК скачать `pickline-windows.zip`, проверить: нет
консоли, есть иконка, окно открывается, подбор мгновенный, в `pickline.log`
нет ошибок.


## 2026-09-14 · mac · поздний вечер: правки по отзывам с ПК

**Сделано:** логотип — тёмная карта Dota (река, базы, линия пиков,
`app/tools/make_icon.py`); дочерний curl на Windows без окна
(`CREATE_NO_WINDOW`) — это и был «пустой PowerShell»; переключатель
«поверх окон» убран (вешал приложение), остался ключ `--on-top`; окно
pywebview с постоянным хранилищем (`private_mode=False`,
`.cache/webview`) — аккаунт и настройки больше не сбрасываются; сборка
предметов: группы-альтернативы (ботинки 1 уровня, апгрейды блинка) — один
в сборке, остальные «или … N%»; про-матчи: перечитывание раз в 10 мин,
кнопка «Обновить», строка свежести с причиной отказа OpenDota
(`net.status`), время матча в карточке.

**Проверено на mac:** альтернативы на Void Spirit (Treads 55% + Phase 36% →
один с альтернативой); свежесть про-матчей; окно с хранилищем открывается.
**На раннере:** все сборки зелёные, последняя — версия `.14`. На ПК
пользователь проверяет по ходу; жалоб на `.12+` пока не было.

**Открыто:** лента про-матчей отстаёт на ~25 ч — это OpenDota (реплей →
разбор → публикация), не приложение. Варианты (STRATZ с токеном
пользователя, Steam Web API, `/live` OpenDota) предложены, пользователь
решил оставить как есть. Хук `session_start.sh` на Windows — по-прежнему
открыт. Бэктест модели — не начат.

**Следующий шаг:** ждать отзывов с ПК по `.14`; при новых задачах —
бэктест модели по про-матчам.


## 2026-09-15 · mac · ночь: один exe, турниры по позициям, позиция «Авто»

**Сделано:** сборка `--onefile` — один `pickline.exe` без папки; данные
приложения переехали в профиль (`app/paths.py`: `%LOCALAPPDATA%\Pickline`,
на macOS `~/Library/Application Support/Pickline`), лог и токен — рядом с
exe; релиз отдаёт сам exe вместо архива. Турниры: фильтр по позиции и
позиции героя под именем (по `positions.json`). Подбор: позиция «Авто» —
вывод из занятых союзниками позиций и истории аккаунта
(`guess_position`), с пояснением на странице.

**Проверено на mac:** пути в обоих режимах, сервер, подбор, портреты;
срезы турниров по позициям на живых данных; автопозиция на четырёх
случаях. **На раннере:** onefile-exe с меткой из папки с кириллицей —
окно открылось, кэш лёг в `%LOCALAPPDATA%\Pickline\.cache`.

**Открыто:** onefile медленнее стартует (распаковка) и чаще нравится
антивирусам — если Defender на ПК ругается, откат на `--onedir` одной
строкой в workflow (paths.py остаётся). Чтение надписи роли с экрана
пользователь отклонил. Хук на Windows, бэктест — без изменений.

**Следующий шаг:** отзыв с ПК по `2026-09-15.1` (один файл, «Авто»).


## 2026-09-16 · mac · день: роль с экрана, установщик, обновления, бэктест

**Сделано:** позиция «Авто» — только по экрану, без аккаунта (пользователь
отверг историю аккаунта): чтение подписей ролей под слотами своей команды
(`app/vision/roles.py`, шаблоны `app/data/roles/*.png` при базовой ширине
1280) + номер своего слота от GSI (`team_slot`); запасной путь — по
союзникам. Сеть: повтор при обрыве TLS/соединения, понятные сообщения
(`describe_error`), пауза 15 мин после неудачного фонового обновления.
Установщик Inno Setup `pickline-setup.exe` (per-user, `{localappdata}\
Programs\Pickline`, ярлыки) — снимает SmartScreen-вопрос при каждом
запуске, который давал onefile-exe с меткой из интернета. Проверка
обновлений: `version.txt` в релизе, `/api/update`, ссылка «Новая версия»
в шапке. Бэктест модели `app/tools/backtest.py` (`pub` — ранговые матчи из
`public_matches` после даты снимка STRATZ, `pro` — турнирные). Итог на
20 000 ранговых: матчапы STRATZ — 57% знака, AUC 0.60, при |edge| 3–5 п.п.
— 71%; база и синергия дублируют силу героя из матчапов и с большим весом
портят оценку → `W_BASE` 0.6→0.2, `W_SYNERGY` 1.0→0.5 (AUC 0.588→0.593).
Матчапы OpenDota — это матчи лиг, на пабах AUC 0.51 (монетка); на про
71%, но внутри выборки. Поправлены README и подсказка источника.

**Проверено на mac:** роли с экрана e2e (скриншот → слоты [3,2,1,4,5] →
слот 2 от GSI → «Лёгкая» → позиция 1); подбор с новыми весами (оба
источника); `/api/update` до и после появления `version.txt`. **На
раннере:** сборка `a8e7e36` зелёная, в релизе `pickline-setup.exe`,
`pickline.exe`, `version.txt` (2026-09-16.3); тихая установка и запуск
установленного exe прошли. Сборка `488f979` упала только на сдвиге тега —
ограничение GITHUB_TOKEN для коммитов с правкой workflow, следующая прошла.

**Открыто:** чтение ролей на ПК вживую (полное разрешение, очередь ролей)
не проверено — шаблоны срезаны со сжатого скриншота. Токены GitHub и
STRATZ из чата стоит отозвать. Хук на Windows — без изменений. Веса
`W_TOUR`/`W_PERSONAL` бэктестом не проверяются (нет разметки).

**Следующий шаг:** отзыв с ПК по `2026-09-16.4` через установщик: нет
вопроса SmartScreen, «Позиция с экрана» в подборе, ссылка на обновление.


## 2026-09-16 · mac · вечер: OpenDota не отвечает — снимки вместо ошибок

**Сделано:** по жалобе с ПК «нет связи с …/explorer: SSL/TLS-соединение не
установилось» (канал до OpenDota/Cloudflare режется на рукопожатии и в
Python, и в curl). Вкладка «Турниры», сборка и прокачка героя больше не
ждут сеть и не падают: свежая памятка → устаревшая памятка/снимок сразу
(+ пересчёт в фоне, страница дозапрашивает) → «считается»; при сбое —
текст причины под данными, а не ошибка вместо них. Новый снимок
`app/data/builds_fallback.json.gz` (сборки и прокачка всех героев, 3 мес,
Premium + Professional; `app/tools/update_builds.py`, CI по пятницам).
Фоновый счёт (`net.compute_in_background`) запоминает ошибку
(`memo_status`) и не повторяет попытку 2 минуты. Кнопка «Обновить» во
вкладке «Турниры». README: снимки, порядок отката, что значит ошибка TLS.

**Проверено на mac:** все шесть маршрутов отвечают за 0.00 с и досчитывают в
фоне; с «мёртвым» источником (исключение вместо базы) сборка/прокачка/
турниры отдают снимок с пометкой, после сбоя pending снят и в note причина.
На ПК с битым каналом — не проверено, ждём отзыв.

**Открыто:** сборка против конкретного драфта на таком канале не появится
никогда — только общая из снимка. Лента про-матчей на нём тоже мёртвая
(кэш). Остальное — как в дневной записи.

**Следующий шаг:** отзыв с ПК по `2026-09-16.5`: «Турниры» и сборка
показывают снимок с пометкой вместо ошибки; работает ли OpenDota через VPN.
